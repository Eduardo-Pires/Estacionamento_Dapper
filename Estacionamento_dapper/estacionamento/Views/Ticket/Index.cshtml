@{
    ViewData["Title"] = "Tickets";
}

<div class="text-center">
    <h1 class="display-4">Tickets</h1>
    <a class="btn btn-primary" asp-area="" asp-controller="Ticket" asp-action="Novo">Novo Ticket</a>
</div>

<div class="text-center">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Data Entrada</th>
                <th>Data Saída</th>
                <th>Valor</th>
                <th>Veículo Id</th>
                <th>Vaga Id</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.DataEntrada</td>
                    <td>@item.DataSaida</td>
                    <td>@item.Valor</td>
                    <td>@item.VeiculoId</td>
                    <td>@item.VagaId</td>
                    <td>
                        <a asp-action="Editar" asp-route-id="@item.Id" asp-controller="Ticket" class="btn btn-warning">Editar</a>
                    </td>
                    <td>
                        <form onsubmit="return confirm('Confirma a exclusão?')" asp-action="Apagar" asp-controller="Ticket" method="post">
                            <input type="hidden" name="id" value="@item.Id">
                            <button type="submit" class="btn btn-danger">Excluir</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
